# ts-express-docker-boilerplate

An express server boilerplate with Typescript in a Docker container.

## Run

1. Development

   `docker compose -f docker-compose.dev.yml up`

2. Production

   `docker compose up`
